// Imports
// __________________________________________________________________________________________

// Core
import styled from "styled-components";

// Constants
import { Theme } from "../../../constants/Theme";

// Helpers

// Begin Styles
// __________________________________________________________________________________________

/**
 *
 * @name AccordionListingsStyle
 *
 */

export const AccordionListingsClassname = "accordion-listings";

export const AccordionListingsStyle = styled.section`
  .${AccordionListingsClassname}__list {
  }

  @media (max-width: ${Theme.Base.Media.Width.Md}) {
  }

  @media (max-width: ${Theme.Base.Media.Width.Md}) {
  }
`;
